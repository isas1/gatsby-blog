import React from "react"
import { graphql, Link } from 'gatsby'

const singleTagIndexTemplate = ({ data }) => {
  return (
    <div>
      <div>
        single tag here
      </div>
    </div>
  );
}

export default singleTagIndexTemplate;